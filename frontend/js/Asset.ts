enum Asset {
  DRESSING = 0,
  RYE = 1,
  SWISS = 2,
  PASTRAMI = 3,
}

const int_to_asset: Asset[] = [
  Asset.DRESSING,
  Asset.RYE,
  Asset.SWISS,
  Asset.PASTRAMI,
];

export { int_to_asset };
export default Asset;
